name: usbipd_win_gui
description: GUI to control usbipd-win

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  fluent_ui: ^3.10.0
  provider: ^6.0.2
  system_theme: ^2.0.0
  window_manager: ^0.2.1
  url_launcher: ^6.0.20
  version: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.1.6
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true

flutter_native_splash:
  color: "#42a5f5"
  image: web/assets/flutter_logo.png